// Generated by CoffeeScript 1.4.0
(function() {

  window.Glow = {};

  /*
  The URL library is responsible for
  generating URLs to framework routes.
  */


  Glow.URL = (function() {

    function URL() {}

    URL.base = "%BASE%";

    URL.asset = "%ASSET%";

    /*
      Retrieve the URL to a route.
      @param  String  uri
      @param  Boolean https
      @return String
    */


    URL.to = function(uri, https) {
      var url;
      if (https == null) {
        https = false;
      }
      url = this.base + "/" + uri;
      if (https) {
        url.replace("http://", "https://");
      }
      return url;
    };

    /*
      Retrieve the URL to an asset
    */


    URL.to_asset = function(uri, https) {
      var url;
      if (https == null) {
        https = false;
      }
      url = this.asset + "/" + uri;
      if (https) {
        url.replace("http://", "https://");
      }
      return url;
    };

    /*
      Retrieve the URL to an action
    */


    URL.to_action = function(action, parameters) {
      var controller, key, parameter, url, _ref;
      if (parameters == null) {
        parameters = {};
      }
      _ref = action.split('@'), controller = _ref[0], action = _ref[1];
      controller = controller.replace('.', '/');
      url = "" + controller + "/" + action;
      for (key in parameters) {
        parameter = parameters[key];
        url += '/' + parameter;
      }
      return this.to(url);
    };

    /*
      Retrieve the secure URL to a route.
    */


    URL.to_secure = function(uri) {
      return this.to(uri, true);
    };

    return URL;

  })();

}).call(this);
