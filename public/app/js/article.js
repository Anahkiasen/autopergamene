// Generated by CoffeeScript 1.6.2
(function() {
  foreach('p a img', function(image) {
    return image.parentNode.classList.add('image-wrap');
  });

  foreach('img, div', function(image) {
    image.removeAttribute('width');
    image.removeAttribute('height');
    return image.removeAttribute('style');
  });

  foreach('p.code', function(code) {
    return code.innerHTML = '<pre>' + code.innerHTML + '</pre>';
  });

  foreach('pre', function(pre) {
    var content;

    content = pre.textContent;
    return Rainbow.color(content, 'php', function(highlighted) {
      return pre.innerHTML = highlighted;
    });
  });

}).call(this);
