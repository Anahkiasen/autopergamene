// Generated by CoffeeScript 1.4.0
(function() {
  var addEvent, link, _i, _len, _ref;

  addEvent = (function() {
    if (document.addEventListener) {
      return function(el, type, fn) {
        return el.addEventListener(type, fn, false);
      };
    }
    return function(el, type, fn) {
      return el.attachEvent("on" + type, function() {
        return fn.call(el, window.event);
      });
    };
  })();

  _ref = document.querySelectorAll('[data-show]');
  for (_i = 0, _len = _ref.length; _i < _len; _i++) {
    link = _ref[_i];
    addEvent(link, 'click', function(link) {
      var block;
      block = document.querySelector(link.target.getAttribute('data-show'));
      return block.classList.toggle('open');
    });
  }

}).call(this);
